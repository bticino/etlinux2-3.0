diff -Naur netkit-tftp-0.17.orig/MCONFIG netkit-tftp-0.17/MCONFIG
--- netkit-tftp-0.17.orig/MCONFIG	Thu Jan  1 01:00:00 1970
+++ netkit-tftp-0.17/MCONFIG	Thu May 19 16:23:29 2005
@@ -0,0 +1,16 @@
+# Generated by configure (confgen version 2) on Thu May 19 16:23:29 CEST 2005
+#
+
+BINDIR=/usr/bin
+SBINDIR=/usr/sbin
+MANDIR=/usr/man
+BINMODE=755
+DAEMONMODE=755
+MANMODE=644
+PREFIX=/usr
+EXECPREFIX=/usr
+INSTALLROOT=
+CC=/opt/snapgear-toolchain/bin/arm-linux-gcc
+CFLAGS=-O2 -Wall -W -Wpointer-arith -Wbad-function-cast -Wcast-qual -Wstrict-prototypes -Wmissing-prototypes -Wmissing-declarations -Wnested-externs -Winline 
+LDFLAGS=
+LIBS=
diff -Naur netkit-tftp-0.17.orig/configure netkit-tftp-0.17/configure
--- netkit-tftp-0.17.orig/configure	Sun Jul 23 07:42:25 2000
+++ netkit-tftp-0.17/configure	Thu May 19 16:23:28 2005
@@ -89,18 +89,22 @@
     fi
     echo "$CC"
 else
-    echo -n 'Checking if C compiler works... '
-    if (
-          $CC __conftest.c -o __conftest || exit 1
-          ./__conftest || exit 1
-       ) >/dev/null 2>&1; then
-         echo 'yes'
-     else
-         echo 'no'
-         echo 'Compiler '"$CC"' does not exist or cannot compile C; try another.'
-         rm -f __conftest*
-         exit
-     fi
+    if [ "$CROSS_COMPILE" = "" ] ; then
+        echo -n 'Checking if C compiler works... '
+        if (
+              $CC __conftest.c -o __conftest || exit 1
+              ./__conftest || exit 1
+           ) >/dev/null 2>&1; then
+             echo 'yes'
+         else
+             echo 'no'
+             echo 'Compiler '"$CC"' does not exist or cannot compile C; try another.'
+             rm -f __conftest*
+             exit
+         fi
+    else # Etlinux cross compilation: force "yes
+	echo yes
+    fi
 fi
 
 echo -n "Checking if $CC accepts gcc warnings... "
